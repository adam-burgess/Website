---
// Wordmark + main navigation. Highlights the section you're in.
const links = [
  { href: '/',        label: 'Home' },
  { href: '/off-the-bench/', label: 'Off the Bench' },
  { href: '/notes/',  label: 'Field Notes' },
];

const path = Astro.url.pathname;
// Every path starts with '/', so Home only counts as current on the homepage itself.
const isCurrent = (href: string) => (href === '/' ? path === '/' : path.startsWith(href));
---
<header class="masthead">
  <div class="wrap">
    <a class="wordmark" href="/">jumari<span>.com.au</span></a>
    <nav aria-label="Main">
      {links.map(({ href, label }) => (
        <a href={href} aria-current={isCurrent(href) ? 'page' : undefined}>{label}</a>
      ))}
    </nav>
  </div>
</header>

<style>
  /* Styles in an .astro file are scoped: they only affect this component. */
  .wrap {
    display: flex; flex-wrap: wrap; align-items: baseline;
    justify-content: space-between; gap: 12px 28px;
    padding-block: 26px 0;
  }
  .wordmark {
    font-family: var(--mono);
    font-size: 13px; letter-spacing: 0.02em;
    color: var(--ink); text-decoration: none;
  }
  .wordmark span { color: var(--ink-3); }
  nav { display: flex; gap: 22px; flex-wrap: wrap; }
  nav a {
    font-size: 14px; color: var(--ink-2); text-decoration: none;
    padding-bottom: 2px; border-bottom: 1px solid transparent;
  }
  nav a:hover,
  nav a[aria-current='page'] { color: var(--ink); border-bottom-color: var(--accent); }
</style>
